/**
 * PostCSS — Tailwind CSS v4 (plugin dedicado).
 * Ver styles/globals.css para el `@import "tailwindcss"` y el `@theme`.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
